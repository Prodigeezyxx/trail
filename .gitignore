# Raw downloaded data — not needed in repo (regenerated via scripts/ingest-layers.py)
data-raw/
*.zip

# Vite
dist/
node_modules/

# Environment
.env
.env.local

# Firebase local cache (deploy config itself is committed)
.firebase/
